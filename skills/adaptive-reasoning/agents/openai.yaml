interface:
  display_name: "自动推理强度"
  short_description: "基于所选模型自动升档并跳过低档，进入 ultra 前建议并询问 Astra 或 Sol"
  default_prompt: "使用 $adaptive-reasoning 继续开发，保持所选模型并跳过 low；需要 ultra 时先建议并询问我选择 Astra 还是 Sol。"

policy:
  allow_implicit_invocation: true
